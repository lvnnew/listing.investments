// В нижнем регистре для обращения к API
export enum ORDER {
  ASC = "asc",
  DESC = "desc",
}

export enum LOCALES {
  DEFAULT = "default",
  EN = "en",
  RU = "ru",
}

export const DEFAULT_LOCALE = LOCALES.EN;
